.row
  .col-lg-6

    = form_for(@expenditure, { class: '' }) do |e|

      = render partial: 'common/form_validation_message', locals: { obj: @expenditure }

      .form-group
        label Name
        = e.text_field(:name, { class: 'form-control' })

      .form-group
        label Date
        = e.text_field(:date, { class: 'form-control datepicker' })

      .form-group
        label Amount
        = e.text_field(:amount, { class: 'form-control' })


      = e.submit({ class: 'btn btn-info' })
      |&nbsp;&nbsp;
      = link_to 'Cancel', expenditures_path, { class: 'btn btn-danger' }